# 参与改进 dev-skill

感谢你帮助改进这个 Skill。最有价值的反馈不是抽象规则，而是 Agent 在真实全栈仓库中的具体失败行为。

## 推荐提交内容

- 原始需求或经过脱敏的等价需求；
- 使用的平台与版本，如 Codex、Cursor 或 Qoder；
- 项目技术栈（前端 / 后端 / 数据库）与相关框架、组件库版本；
- Agent 实际采取了什么错误或低效行为；
- 预期行为，以及相关 Diff、日志、SQL、接口响应或截图；
- 问题是否能稳定复现。

提交前请删除密钥、内部地址、客户数据、生产连接串和其他敏感信息。

## 修改原则

- 优先修正已观察到的真实失败，不为假设场景堆叠通用规则；
- 保持 `SKILL.md` 聚焦，把条件性细节放入对应的 `references/` 或 `examples/`；
- 不绑定某个平台独有的工具名，除非内容明确限定该平台；
- 不削弱安全门禁：数据库默认只读、高风险操作确认、保护用户未提交修改等约束不得弱化；
- 修改后检查相对链接，并在本地执行 `pnpm docs:build`，确认文档站可构建；
- README、在线文档站与安装路径、调用语法的关键事实必须一致；README 是 GitHub 仓库的内容源，`index.md` 是文档站首页。

## Pull Request

请在 PR 中简要说明：

1. 观察到了什么问题；
2. 为什么当前规则无法处理；
3. 修改了哪些行为；
4. 如何验证该修改不会扩大无关任务的约束，也不会降低安全门禁。
