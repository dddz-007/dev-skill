# 验证与异常恢复

验证顺序按变更选择：实际 Diff → 语法/静态/类型检查 → 相关测试 → 构建 → 接口或页面实际验证 → 回归检查。超时不算通过；未执行的检查必须说明原因。

发生乱码、非法编码或补丁失败时，立即停止对文件编辑，从事故前工作区、版本记录或可信来源恢复，确认原文后再用局部补丁重做；不得猜测乱码。

发生命令失败时保留关键错误、定位原因、做针对性修复并重跑；若失败来自环境且代码无从验证，写明原因、影响和手工步骤。不要用“命令成功退出”替代功能证据。
